{
  "name": "my-fat-senator",
  "version": "1.0.0",
  "workspaces": [
    "lib",
    "packages/*"
  ],
  "dependencies": {
    "prisma": "^5.14.0"
  },
  "description": "A monorepo for the My Fat Senator project",
  "scripts": {
    "build": "tsc --build --verbose",
    "test": "echo \"Error: no test specified\" && exit 1",
    "import": "npm run start --workspace=@my-fat-senator/importer"
  },
  "author": "nym",
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.14",
    "@types/node": "^22.5.4",
    "daisyui": "^4.12.10",
    "dotenv": "^16.4.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  }
}
