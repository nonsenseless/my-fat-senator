{
  "name": "@my-fat-senator/importer",
  "version": "1.0.0",
  "description": "Console project for importing data from congress.gov, census.gov, and congressional vote data.\"",
  "main": "main.ts",
  "scripts": {
    "build": "tsc --build",
    "start": "ts-node ./main",
    "votes": "ts-node ./main --command votes",
    "legislators": "ts-node ./main --command legislators",
    "process-depictions": "ts-node ./main --command process-depictions",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "nym",
  "license": "ISC",
  "devDependencies": {
    "@types/minimist": "^1.2.5",
    "minimist": "^1.2.8",
    "ts-node": "^10.9.2"
  },
  "dependencies": {
    "@my-fat-senator/lib": "^1.0.0"
  }
}
